{
  "name": "lerna",
  "private": true,
  "workspaces": [
      "packages/*"
  ],
  "scripts": {
      "build": "yarn workspaces run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
